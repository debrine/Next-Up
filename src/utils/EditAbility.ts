export function EditAbility(keyID: string, ability: AbilityListTypes) {
  // Will configure when I get to that point to see exactly what's needed. This may just end up being edited by react-hook-form.
  console.log(keyID);
  console.log(ability);
}
